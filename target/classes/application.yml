spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reppy
    username: reppyuser
    password: strongpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

